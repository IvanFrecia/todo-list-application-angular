// unocss.config.ts
import { defineConfig } from 'unocss';

export default defineConfig({
  // Add your Unocss shortcuts, rules, presets etc.
  shortcuts: {
    'btn': 'px-4 py-2 rounded font-semibold',
  }
});
